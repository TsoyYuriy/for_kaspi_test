# Используем официальный образ Node.js версии 16 на основе Alpine Linux
FROM node:alpine

# Устанавливаем рабочую директорию в контейнере
WORKDIR /backend_app

# Копируем файл package.json в рабочую директорию
COPY package.json .

# Устанавливаем зависимости, указанные в package.json
RUN npm install

# Копируем весь исходный код в рабочую директорию
COPY . .

# Открываем порт 4000 для доступа к приложению
EXPOSE 9001

# Указываем команду по умолчанию для запуска приложения
CMD ["npm", "run", "dev"]